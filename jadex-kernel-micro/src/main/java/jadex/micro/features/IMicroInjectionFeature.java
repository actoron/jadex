package jadex.micro.features;

/**
 *  Inject agent object, arguments, results and features into annotated field values.
 *  Required services are added using a different feature at another point in the init process (i.e. after sub components).
 */
public interface IMicroInjectionFeature
{
	// no API methods
}
