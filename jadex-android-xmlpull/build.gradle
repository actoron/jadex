dependencies
{
    provided libraries.android
    compile(project(':jadex-xml')) {
        exclude module: 'bcprov-jdk15'
    }
//    testCompile(project(':jadex-commons:test-jar'))
    testCompile files(project(':jadex-commons').sourceSets.test.output)
}