description 'This Project includes a variant of the Jadex XML Parser that uses the Android XML Pull API.'

dependencies
{
    compile libraries.android
    compile(project(':jadex-xml'))
//    testCompile project(':jadex-commons').sourceSets.test.output
    testCompile(project(':jadex-xml'))
    testCompile project(path: ':jadex-traverser', configuration: 'testOutput')
//    testCompile project(path: ':jadex-commons', configuration: 'testOutput')
//    testCompile files(project(':jadex-commons').sourceSets.test.output)
}