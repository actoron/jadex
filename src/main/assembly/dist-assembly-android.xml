<assembly
	xmlns="http://maven.apache.org/plugins/maven-assembly-plugin/assembly/1.1.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/plugins/maven-assembly-plugin/assembly/1.1.0 http://maven.apache.org/xsd/assembly-1.1.0.xsd">

	<id>androiddist</id>

	<formats>
		<format>zip</format>
	</formats>

	<includeBaseDirectory>true</includeBaseDirectory>

	<moduleSets>
		<moduleSet>
			<excludes>
				<exclude>net.sourceforge.jadex:jadex-android-example-project-maven</exclude>
				<exclude>net.sourceforge.jadex:jadex-android-integration-test</exclude>
			</excludes>
			<!-- Selected by profile. <includes> <include>jadex:*</include> </includes> -->
			<binaries>
				<includes>
					<include>jadex:*</include>
				</includes>
				<outputFileNameMapping>${artifact.artifactId}-${artifact.version}${dashClassifier?}.${artifact.extension}</outputFileNameMapping>
				<!-- <outputFileNameMapping>${module.artifactId}-${module.version}${dashClassifier?}.${module.extension}</outputFileNameMapping> -->
				<!-- <outputFileNameMapping>${artifactId}-${version}${dashClassifier?}.${extension}</outputFileNameMapping> -->
				<outputDirectory>lib</outputDirectory>
				<unpack>false</unpack>
			</binaries>
			<sources>
			<!-- Copies library properties to dist -->
				<excludes>
					<exclude>**/*</exclude>
				</excludes>
				<fileSets>
					<fileSet>
						<directory>target</directory>
						<includes>
							<include>*.properties</include>
						</includes>
					</fileSet>
				</fileSets>
				<outputDirectoryMapping>lib/</outputDirectoryMapping>
			</sources>

		</moduleSet>
	</moduleSets>

	<files>
		<file>
			<source>target/sources.zip</source>
			<outputDirectory>/</outputDirectory>
		</file>
		<file>
			<source>target/jadex-android-example-projects.zip</source>
			<outputDirectory>/</outputDirectory>
		</file>
	</files>

</assembly>
