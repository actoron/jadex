description 'Binary serialization support for Jadex.'

dependencies 
{
	api project(':serialization:jadex-traverser')
	api project(':util:jadex-commons')
	
	// Correct(?) way: (project dependency correctly interpreted by eclipse import as well as gradle build)
	testImplementation project(path: ':serialization:jadex-traverser', configuration: 'testOutput')
}
