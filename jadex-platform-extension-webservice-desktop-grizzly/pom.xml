<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<artifactId>jadex-platform-extension-webservice-desktop-grizzly</artifactId>
	<packaging>jar</packaging>
	<name>jadex-platform-extension-webservice-desktop-grizzly</name>
	<parent>
		<groupId>net.sourceforge.jadex</groupId>
		<artifactId>jadex-parent</artifactId>
		<version> 3.0-SNAPSHOT </version>
		<relativePath>../jadex-parent/pom.xml</relativePath>
	</parent>

	<description>
		The Jadex desktop webservice platform extension package contains
		functionality for using and providing WSDL web services and REST web services.
		It uses dependencies which are only available for Java SE.
	</description>
	
	<properties>
		<grizzly.version>2.3.22</grizzly.version>
		<jersey.version>2.11</jersey.version>
	</properties>


	<dependencies>
		<dependency>
			<groupId>net.sourceforge.jadex</groupId>
			<artifactId>jadex-platform-extension-webservice-desktop</artifactId>
			<version>${project.version}</version>
		</dependency>
	
  		<dependency>
     		<groupId>org.glassfish.grizzly</groupId>
     		<artifactId>grizzly-http-servlet</artifactId>
     		<version>${grizzly.version}</version>
		</dependency>
		<dependency>
     		<groupId>org.glassfish.grizzly</groupId>
     		<artifactId>grizzly-http-server-multipart</artifactId>
     		<version>${grizzly.version}</version>
		</dependency>
		<dependency>
			<groupId>org.glassfish.grizzly</groupId>
			<artifactId>grizzly-http-server</artifactId>
			<version>${grizzly.version}</version>
		</dependency>
		
		<dependency>
			<groupId>org.glassfish.jersey.media</groupId>
			<artifactId>jersey-media-multipart</artifactId>
			<version>${jersey.version}</version>
		</dependency>
		<dependency>
			<groupId>org.glassfish.jersey.containers</groupId>
			<artifactId>jersey-container-grizzly2-http</artifactId>
			<version>${jersey.version}</version>
		</dependency>
		<dependency>
    		<groupId>org.glassfish.jersey.core</groupId>
    		<artifactId>jersey-client</artifactId>
    		<version>${jersey.version}</version>
		</dependency>
		<dependency>
		    <groupId>org.glassfish.jersey.media</groupId>
		    <artifactId>jersey-media-moxy</artifactId>
		    <version>${jersey.version}</version>
		</dependency>
				
	</dependencies>
	
	<!-- Set scopes of standard jadex jars to provided for excluding them from dist -->
	<profiles>
		<profile>
			<id>dist</id>
			<dependencies>
				<dependency>
					<groupId>javassist</groupId>
					<artifactId>javassist</artifactId>
					<version>3.12.1.GA</version>
					<scope>provided</scope>
				</dependency>
				
				<dependency>
					<groupId>javax.activation</groupId>
					<artifactId>activation</artifactId>
					<version>1.1</version>
					<scope>provided</scope>
				</dependency>
				
				<dependency>
					<groupId>net.sourceforge.jadex</groupId>
					<artifactId>jadex-platform</artifactId>
					<version>${project.version}</version>
					<scope>provided</scope>
				</dependency>
		
				<dependency>
					<groupId>net.sourceforge.jadex</groupId>
					<artifactId>jadex-kernel-component</artifactId>
					<version>${project.version}</version>
					<scope>provided</scope>
				</dependency>
			</dependencies>
		</profile>
	</profiles>
</project>