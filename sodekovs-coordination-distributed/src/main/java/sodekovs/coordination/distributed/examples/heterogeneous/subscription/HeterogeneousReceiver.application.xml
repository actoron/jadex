<!-- <H3>Heterogeneous agents example application for V2 Deco4MAS Distributed</H3> 
	Heterogeneous agents example: This example shows how different agent types 
	(micro and BDI) could be coordinated using the framework. The 'SayHelloStep' 
	IComponentStep in the example micro agent and the 'sayhello' goal in the 
	example BDI agent are observed by the framework and the events are published 
	over the coordination medium to the 'receivehello' internal event in the 
	example BDI agent and the 'ReceiveHelloStep' IComponentStep in the exmaple 
	micro agent. Both observed events pass a parameter message as a string to 
	the received agents. So two by the framework observed events at the sender 
	side result in four triggered events at the receiver side. This is the receiver 
	part of the application for the distribution test. In this part only the 
	receiving Micro Agent is started. -->

<componenttype xmlns="http://jadex.sourceforge.net/jadex"
	xmlns:env="http://jadex.sourceforge.net/jadex-envspace" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://jadex.sourceforge.net/jadex-application 
	                    http://jadex.sourceforge.net/jadex-application-2.0.xsd	                    
	                    http://jadex.sourceforge.net/jadex-envspace 
	                    http://jadex.sourceforge.net/jadex-envspace-2.0.xsd"
	name="HeterogeneousReceiver"
	package="sodekovs.coordination.distributed.examples.heterogeneous.subscription">

	<imports>
		<import>jadex.extension.envsupport.environment.space2d.*</import>
		<import>jadex.extension.envsupport.environment.*</import>
		<import>jadex.extension.envsupport.math.*</import>
		<import>jadex.extension.envsupport.dataview.*</import>
		<import>jadex.extension.envsupport.observer.perspective.*</import>
		<import>jadex.bdi.planlib.*</import>
		<import>jadex.bridge.service.*</import>
		<import>jadex.commons.future.*</import>
		<import>deco4mas.distributed.coordinate.interpreter.coordination_information.*
		</import>
		<import>deco4mas.distributed.coordinate.environment.*</import>
		<import>deco4mas.distributed.annotation.agent.*</import>
		<import>deco4mas.distributed.coordinate.*</import>
		<import>sodekovs.coordination.distributed.examples.heterogeneous.subscription.*
		</import>
	</imports>

	<arguments>
		<!--Context ID. -->
		<argument name="CoordinationContextID" class="String">"sodekovs-1"
		</argument>
	</arguments>


	<extensiontypes>
		<env:envspacetype name="coordspace" width="11"
			height="11" class="CoordinationSpace">
			<env:property name="dynamics_configuration">"sodekovs-coordination-distributed/src/main/java/sodekovs/coordination/distributed/examples/heterogeneous/subscription/coordination.dynamics.xml"
			</env:property>

			<env:spaceexecutor class="DeltaTimeExecutor">
				<env:property name="space">$space</env:property>
				<env:property name="tick">true</env:property>
			</env:spaceexecutor>
		</env:envspacetype>
	</extensiontypes>

	<componenttypes>
		<componenttype name="ExampleMicro"
			filename="sodekovs/coordination/distributed/examples/heterogeneous/subscription/ExampleMicroAgent.class" />
	</componenttypes>


	<configurations>
		<configuration name="HeterogeneousReceiver">
			<extensions>
				<env:envspace name="mycoordspace" type="coordspace"
					width="2" height="2" />
			</extensions>
			<components>
				<component type="ExampleMicro" number="1" />
			</components>
		</configuration>
	</configurations>

</componenttype>