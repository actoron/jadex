<project	xmlns="http://maven.apache.org/POM/4.0.0"
			xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
			xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

	<modelVersion>4.0.0</modelVersion>
	<groupId>mygroup</groupId>
	<artifactId>jadex-example-project</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	
	<properties>
		<!-- Specify desired Jadex version here. -->
		<jadex.version>2.1-SNAPSHOT</jadex.version>
	</properties>
	
	<!-- Snapshot repository (only required when using snapshot version of Jadex) -->
	<repositories>
		<repository>
			<id>snapshots</id>
			<url>https://oss.sonatype.org/content/repositories/snapshots/</url>
		</repository>
	</repositories>
	
	<build>
		<resources>
			<!-- Allow ADF files (.xml, .bpmn, ...) be placed in source folder -->
			<resource>
				<directory>src/main/java</directory>
				<excludes>
					<exclude>**/*.java</exclude>
					<exclude>**/.svn/*</exclude>
				</excludes>
			</resource>
		</resources>
	</build>

	<dependencies>
		<!-- Platform for execution -->
		<dependency>
			<groupId>net.sourceforge.jadex</groupId>
			<artifactId>jadex-platform-standalone</artifactId>
			<version>${jadex.version}</version>
		</dependency>
		
		<!-- Component kernel required for basic platform components -->
		<dependency>
			<groupId>net.sourceforge.jadex</groupId>
			<artifactId>jadex-kernel-component</artifactId>
			<version>${jadex.version}</version>
		</dependency>
		
		<!-- Micro kernel for hello world agent -->
		<dependency>
			<groupId>net.sourceforge.jadex</groupId>
			<artifactId>jadex-kernel-micro</artifactId>
			<version>${jadex.version}</version>
		</dependency>
		
		<!-- Platform administration tools (JCC) -->
		<dependency>
			<groupId>net.sourceforge.jadex</groupId>
			<artifactId>jadex-runtimetools-swing</artifactId>
			<version>${jadex.version}</version>
		</dependency>
	</dependencies>
</project>