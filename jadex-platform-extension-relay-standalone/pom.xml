<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<artifactId>jadex-platform-extension-relay-standalone</artifactId>
	<packaging>jar</packaging>
	<name>jadex-platform-extension-relay-standalone</name>
	<parent>
		<groupId>net.sourceforge.jadex</groupId>
		<artifactId>jadex-parent-pro</artifactId>
		<version> 3.0-SNAPSHOT </version>
		<relativePath>../jadex-parent-pro/pom.xml</relativePath>
	</parent>

	<description>
		Standalone server application for Jadex relay.
	</description>

	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-jar-plugin</artifactId>
        		<version>${maven-jar-plugin.version}</version>
				<configuration>
					<archive>
						<manifest>
							<addClasspath>true</addClasspath>
							<useUniqueVersions>false</useUniqueVersions>
							<mainClass>jadex.base.relay.StandaloneRelay</mainClass>
						</manifest>
					</archive>
				</configuration>
			</plugin>
		</plugins>
	</build>
	
	<dependencies>
	    <dependency>
	        <groupId>net.sourceforge.jadex</groupId>
	        <artifactId>jadex-platform-extension-relay</artifactId>
	        <version>${project.version}</version>
	    </dependency>
	</dependencies>

	<!-- Minimize dependencies for distribution. -->
	<profiles>
		<profile>
			<id>dist</id>
			<dependencies>
			    <!-- 
				<dependency>
					<groupId>javassist</groupId>
					<artifactId>javassist</artifactId>
					<version>3.12.1.GA</version>
					<scope>provided</scope>
				</dependency>
				 -->
				<dependency>
					<groupId>javax.mail</groupId>
					<artifactId>mail</artifactId>
					<version>1.4.1</version>
					<scope>provided</scope>
				</dependency>
				
   			    <!-- Hack!!! Micro/base kernel included in platform (standalone). -->
				<dependency>
					<groupId>net.sourceforge.jadex</groupId>
					<artifactId>jadex-kernel-micro</artifactId>
					<version>${project.version}</version>
					<scope>provided</scope>
				</dependency>
				<dependency>
					<groupId>net.sourceforge.jadex</groupId>
					<artifactId>jadex-kernel-base</artifactId>
					<version>${project.version}</version>
					<scope>provided</scope>
				</dependency>
				<dependency>
					<groupId>net.sourceforge.jadex</groupId>
					<artifactId>jadex-javaparser</artifactId>
					<version>${project.version}</version>
					<scope>provided</scope>
				</dependency>
				<dependency>
					<groupId>org.apache.derby</groupId>
					<artifactId>derby</artifactId>
					<version>10.8.2.2</version>
					<scope>provided</scope>
				</dependency>
				<dependency>
					<groupId>org.ow2.util</groupId>
					<artifactId>util-geolocation-ejb</artifactId>
					<version>1.0.31</version>
					<scope>provided</scope>
				</dependency>
			</dependencies>
		</profile>
	</profiles>
</project>
