description 'Jadex bridge is a base package for kernels and platforms, i.e. it is used by both and provides commonly used interfaces and classes for active components and their management.'

dependencies
{
	compile project(':jadex-commons')
	compile project(':jadex-javaparser')
	compile project(':jadex-bytecode-tools')
	compile project(':jadex-rules-eca') // Remove this stuff, don't know how :-(

	compile 'junit:junit:4.11'	// required for component test suite classes
}
