package jadex.bridge.service.types.factory;

/**
 *  The extension loader service provides no methods,
 *  because currently extensions must be loaded at startup. 
 */
// Todo: support reload of kernels when extensions are added at runtime.
public interface IExtensionLoaderService
{

}
