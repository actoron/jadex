description 'Jadex udp transport'

apply plugin: 'java'

dependencies
{
	api project(':util:udp') 
	api project(':platform:base')
	api project(':util:security')
}

jar
{
	archiveBaseName = project.name
}
