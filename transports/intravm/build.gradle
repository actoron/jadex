description 'Message transport for distributed tests with simulation (no external threads) based on intra VM passing of message data.'

dependencies
{
	api project(':platform:bridge')
	api project(':kernels:micro')
	api project(':transports:base')
}
