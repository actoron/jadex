plugins {
	id "maven-publish" // PublishToMavenLocal
	id "java"
	id "maven"
	id "eclipse"
	//id "eclipse-wtp"
}

description 'Jadex websocket webservice implementation for Nano'

dependencies
{
	api project(':distributions:minimal')
	api project(':serialization:json')
	//runtime project(':platform:webservice-websocket')
	
	//api 'javax.servlet:javax.servlet-api:3.1.0'
	api 'javax.websocket:javax.websocket-api:1.1'
}

