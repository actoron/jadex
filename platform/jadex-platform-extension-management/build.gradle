description 'Services that allow spawning new platforms and updating running platforms to new versions.'

dependencies
{
	api project(':util:jadex-commons')
	api project(':platform:jadex-bridge')
	api project(':serialization:jadex-xml')
	api project(':kernels:jadex-kernel-micro')
	api project(':kernels:jadex-kernel-bpmn')
	api project(':rules:jadex-rules-eca')
	api project(':platform:jadex-platform')
	api project(':tools:jadex-tools-base-swing')
	//api project(':transports:jadex-platform-extension-relaytransport')

	
	// Add transports, otherwise test fails due to consistency check.
	//testImplementation project(':transports:jadex-platform-extension-relaytransport')
	//testImplementation project(':transports:jadex-platform-extension-websockettransport')
	// required for test suite (why?)
	testImplementation project(':kernels:jadex-kernel-component')

	api 'commons-daemon:commons-daemon:1.0.10'
	//apiNoAndroidRuntime 'javassist:javassist:3.12.1.GA'
}
