<?xml version="1.0" encoding="UTF-8"?>
<!--
	<H3>Test agent creation performance.</H3>

	Creates a number of peers and prints out the time/memory it takes.
-->
<agent xmlns="http://jadex.sourceforge.net/jadex"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://jadex.sourceforge.net/jadex
	                    http://jadex.sourceforge.net/jadex-bdi-2.0.xsd"
	name="HelloWorld"
	package="jadex.android.applications.demos.bdi">

	<imports>
		<import>jadex.bridge.service.types.cms.*</import>
		<import>jadex.bridge.service.types.context.*</import>
	</imports>

	<beliefs>
		<!-- <belief name="delay" class="int">
			<fact>0</fact>
		</belief> -->
		<belief name="service" class="boolean" exported="true">
			<fact>true</fact>
		</belief>
		<!-- Parallel execution gains only performance advantages if platform executes
			agents also in parallel (standalone -> async platform executor) -->
		<belief name="parallel" class="boolean" exported="true">
			<fact>false</fact>
		</belief>
		<belief name="HelloMessage" class="String" exported="true">
			<fact>"Hello, this is a BDI Agent speaking."</fact>
		</belief>
	</beliefs>
	
	<plans>
		<plan name="say_hello">
			<body class="SayHelloPlan" />
		</plan>
	</plans>
	
	<services>
		<providedservice name="displayTextService" class="IDisplayTextService">
			<implementation class="DisplayTextServiceImpl"/>
		</providedservice>
		<requiredservice name="cms" class="IComponentManagementService">
			<binding scope="platform"/>
		</requiredservice>
		<requiredservice name="context" class="IContextService">
			<binding scope="platform"/>
		</requiredservice>
	</services>
	
	<properties>
		<!-- <property name="logging.level">java.util.logging.Level.WARNING</property> -->
		<property name="debugging">false</property>
	</properties>

	<configurations>
		<configuration name="default">
			<plans>
				<initialplan ref="say_hello"/>
			</plans>
		</configuration>
		<configuration name="def2"/>
		<!--  <configuration name="capability">
			<beliefs>
				<initialbelief ref="service">
					<fact>false</fact>
				</initialbelief>
			</beliefs>
			<plans>
				<initialplan ref="start_peer"/>
			</plans>
		</configuration>-->
	</configurations>
</agent>

