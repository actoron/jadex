
	<!--
		<H3>Environment / Observer Agent</H3> Used to maintain the GUI /
		Observer.
	-->

<agent xmlns="http://jadex.sourceforge.net/jadex" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://jadex.sourceforge.net/jadex
	                    http://jadex.sourceforge.net/jadex-bdi-2.0.xsd"
	name="ControlCenter" package="sodekovs.investigation.controlcenter">

	<imports>		
		<import>jadex.base.fipa.*</import>
		<import>java.util.Map</import>
		<import>java.util.HashMap</import>
		<import>sodekovs.investigation.model.*</import>
		<import>jadex.commons.*</import>
		<import>jadex.bridge.service.*</import>
		<import>jadex.commons.future.*</import>
	</imports>


	<capabilities>
		<!--
			this capability provides easy access to "out-of-the-box"-plans
			allowing - for example - to register at the DF.
		-->
		<capability name="dfcap" file="jadex.bdi.planlib.df.DF" />
		<!-- Capability to start other agents. -->
		<capability name="cmscap" file="jadex.bdi.planlib.cms.CMS" />
	</capabilities>

	<beliefs>		
		<!--  Reference to the ControlCenter GUI -->
		<belief name="controlCenter" class="ControlCenter"/>
		
		<!--  The parsed simulation configuration xml file. -->
		<belief name="investigationConf" class="InvestigationConfiguration" />
	</beliefs>

	<goals>
	<!-- Predefined Goals.-->
		<!-- Used to find other agents. -->
		<achievegoalref name="cms_search_components">
			<concrete ref="cmscap.cms_search_components" />
		</achievegoalref>
		<!-- Used to search for the dealer. -->
		<achievegoalref name="df_search">
			<concrete ref="dfcap.df_search" />
		</achievegoalref>
		<!-- Used to start other agents. -->
		<achievegoalref name="cms_create_component">
			<concrete ref="cmscap.cms_create_component" />
		</achievegoalref>
		<!-- Used to stop other agents. -->
		<achievegoalref name="cms_destroy_component">
			<concrete ref="cmscap.cms_destroy_component" />
		</achievegoalref>
		<!-- Keep agent services registered at DF. -->
		<maintaingoalref name="df_keep_registered">
			<concrete ref="dfcap.df_keep_registered" />
		</maintaingoalref>

	</goals>

	<plans>		
		<plan name="init_gui">
			<body class="StartControlCenterPlan" />			
		</plan>
	</plans>
	

	<properties>
		<!--
			The environment may throw exceptions when executing property
			listeners and these listeners are not valid any longer. This leads to
			normal plan failure and can therefore be ignored.
		-->
		<property name="logging.level">java.util.logging.Level.WARNING</property>
		<property name="debugging">false</property>
		<property name="dfservice" class="IFuture">SServiceProvider.getService($scope.getServiceProvider(), IDF.class,RequiredServiceInfo.SCOPE_PLATFORM)</property>
	</properties>

	<configurations>
		<configuration name="default">
			<goals>
				<initialgoal ref="df_keep_registered">
					<parameter ref="description">
						<value>
						$propertybase.getProperty("dfservice").createDFComponentDescription(null, $propertybase.getProperty("dfservice").createDFServiceDescription("simulation_control_center_agent","receive_simulation_run_updates", "University of Hamburg"))						
						</value>						
					</parameter>
					<parameter ref="leasetime">
						<value>20000</value>
					</parameter>
				</initialgoal>
			</goals>
			<plans>
				<!-- <initialplan ref="start_sim_experiments" />-->
				<initialplan ref="init_gui" />
			</plans>
		</configuration>
	</configurations>

</agent>