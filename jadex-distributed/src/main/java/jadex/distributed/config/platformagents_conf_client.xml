<!--
  Standalone platform properties.
-->
<properties xmlns="http://jadex.sourceforge.net/jadexconf"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://jadex.sourceforge.net/jadexconf 
	                    http://jadex.sourceforge.net/configuration.xsd"
	name="standalone_conf">
	
	<properties type="platform_standalone" name="daniel">
		<!-- %%% so sah es vor der Umstellung von platformagents_conf zu platformcomponents_conf aus -->
  		<!-- <property type="daemoncomponent" name="cms">jadex/bdi/cmsagent/CMS.agent.xml</property> -->
		<!-- <property type="daemoncomponent" name="df">jadex/bdi/dfagent/DF.agent.xml</property> -->
		<!-- <!- Die Client-Plattform braucht keine GUI, und daher auch keinen JCC.agent.xml, der eine entsprechende aufbauen würde. -> -->
 		<!-- <!- <property type="component" name="jcc">jadex/tools/jcc/JCC.agent.xml</property> -> -->
 		
 		<!-- %%% und sieht es nach der Umstellung aus %%% -->
 		<!-- wait a minute: wozu brauche ich den CMS und DF agent? Ohne einen
 		     JCC machen diese keinen Sinn, da ich ja auch programatisch alles
 		     ansteuern kann oder brauchen etwas die anderen Agenten auf der
 		     Platform den CMS und den DF Agenten damit diese z.B. die Erzeugung
 		     anderer Agenten anstoßen können und keine programatische
 		     Schnittstellt dafür haben? -->
 		
 		<!-- TJA, ohne auch nur einen Agenten sieht die platform_conf für den
 		     client schon sehr leer aus; wenn sich das ändern sollte: wirf
 		     einen Blick in die Version für den server, um zu sehen wie Agenten
 		     für die Platform beim startup eingefügt werden können. -->
 		
	</properties>
</properties>
