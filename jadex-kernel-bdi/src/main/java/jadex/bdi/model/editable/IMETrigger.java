package jadex.bdi.model.editable;

import jadex.bdi.model.IMTrigger;

/**
 *  Editable interface for a basic trigger (e.g. waitqueue).
 */
public interface IMETrigger	extends IMTrigger, IMEElement
{
	todo
}
