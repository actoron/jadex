description 'JSON support for Jadex.'

//apply plugin: 'maven-publish'

dependencies 
{
	api project(':jadex-commons')
	api project(':jadex-traverser')
	
	// Wrong way: (doesn't work for eclipse, depends on generated jar from gradle build)
//	testImplementation files(project(':jadex-traverser').sourceSets.test.output)
//	testImplementation files(project(':jadex-traverser').sourceSets.test.runtimeClasspath)
	
	// Correct(?) way: (project dependency correctly interpreted by eclipse import as well as gradle build)
	testImplementation project(path: ':jadex-traverser', configuration: 'testOutput')

	api 'com.eclipsesource.minimal-json:minimal-json:0.9.4'
	
	//api 'com.google.code.gson:gson:2.3.1'
	//api 'com.thoughtworks.xstream:xstream:1.4.8'
}

//publishing 
//{
//   publications 
//    {
//        mavenJava(MavenPublication) 
//        {
//            from components.java
//        }
//    }
//}
