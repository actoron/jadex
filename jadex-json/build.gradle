description 'JSON support for Jadex.'

//apply plugin: 'maven-publish'

dependencies 
{
	compile project(':jadex-commons')
	compile project(':jadex-traverser')
	
	// Wrong way: (doesn't work for eclipse, depends on generated jar from gradle build)
//	testCompile files(project(':jadex-traverser').sourceSets.test.output)
//	testCompile files(project(':jadex-traverser').sourceSets.test.runtimeClasspath)
	
	// Correct(?) way: (project dependency correctly interpreted by eclipse import as well as gradle build)
	testCompile project(path: ':jadex-traverser', configuration: 'testOutput')

	compile 'com.eclipsesource.minimal-json:minimal-json:0.9.4'
	
	//compile 'com.google.code.gson:gson:2.3.1'
	//compile 'com.thoughtworks.xstream:xstream:1.4.8'
}

//publishing 
//{
//   publications 
//    {
//        mavenJava(MavenPublication) 
//        {
//            from components.java
//        }
//    }
//}
